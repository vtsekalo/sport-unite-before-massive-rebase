import { Button, styled } from '@mui/material';

const RegistrationButton = styled(Button)`
  border-radius: 4px;
  font-weight: 500;
  font-size: 0.9375rem;
  line-height: 26px;
  letter-spacing: 0.46px;
`;

export const Styled = {
  RegistrationButton,
};
