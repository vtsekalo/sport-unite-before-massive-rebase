import { styled } from '@mui/material/styles';

export const CardWrapper = styled('button')(() => ({
  all: 'unset',
  display: 'inline-flex',
  alignItems: 'center',
  justifyContent: 'center',
  cursor: 'pointer',
}));
